import * as PIXI from 'pixi.js';

function createMenuBar(app) {
    cost menuContainer = new PIXI.Container();
    menuContainer.y = 10;
    menuContainer.x = 10;
}

const menuOptions = [
    { label: "Option 1", onClick: ()}
]